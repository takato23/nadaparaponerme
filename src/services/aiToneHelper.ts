/**
 * AI Tone Helper (re-export from root services)
 * 
 * This module re-exports the AI tone helper for unified imports.
 */

export {
    clearToneCache,
    setTonePreference,
    getToneInstructions,
} from '../../services/aiToneHelper';
