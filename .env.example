# Supabase Configuration
# This is now the canonical env template. Copy it to `.env.local` (or `.env`) and never commit your secrets; `.env.local.example` was removed.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# MercadoPago Configuration
# Public Key: Safe to expose in frontend (used for checkout initialization)
VITE_MERCADOPAGO_PUBLIC_KEY=APP_USR-your-public-key-here

# Release Controls (V1)
# Recommended for the first production launch:
VITE_V1_SAFE_MODE=true
VITE_PAYMENTS_ENABLED=false
VITE_REWARDED_ADS_ENABLED=false
VITE_USD_ENABLED=false
VITE_REWARDED_ADS_PROVIDER=AdMob
VITE_ADSENSE_CLIENT_ID=ca-pub-xxxxxxxxxxxxxxxx
VITE_ADSENSE_HOME_SLOT=0000000000
VITE_ADSENSE_CREDITS_SLOT=0000000001

# App Configuration
# Used for OAuth redirects (Google) and a few callback URLs.
# In dev with Vite this is typically 5173. In production set it to your public domain.
VITE_APP_URL=http://localhost:5173

# Paddle (International payments - Merchant of Record)
# Client token (safe for frontend): used by Paddle.js on /pay
VITE_PADDLE_CLIENT_TOKEN=your-client-token-here

# Google Analytics 4 (optional)
# Create a GA4 property at https://analytics.google.com and get the Measurement ID
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Gemini AI (for Edge Functions - set via supabase secrets set)
# GEMINI_API_KEY=your-gemini-api-key
# MERCADOPAGO_ACCESS_TOKEN=APP_USR-your-access-token
# MERCADOPAGO_WEBHOOK_SECRET=your-mercadopago-webhook-signing-secret
# MERCADOPAGO_WEBHOOK_TOKEN=your-random-webhook-token
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# SERVICE_ROLE_KEY=alternate name for SUPABASE_SERVICE_ROLE_KEY (some scripts, incl. Supabase CLI, expect this)
# PADDLE_API_KEY=your-paddle-api-key
# PADDLE_WEBHOOK_SECRET_KEY=your-paddle-webhook-secret
# PADDLE_PRICE_ID_PRO=pri_...
# PADDLE_PRICE_ID_PREMIUM=pri_...
# APP_URL=https://your-public-domain.com
# APP_URL_ALLOWLIST=http://localhost:5173,http://127.0.0.1:5173,https://your-project.vercel.app
# Closed beta allowlist (Edge only; comma-separated emails)
# BETA_ALLOWLIST_EMAILS=user1@gmail.com,user2@gmail.com
# Security hardening (Edge)
# SECURITY_FAIL_CLOSED_HIGH_COST=true
# ALLOWED_WEB_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-app.com
# PROXY_IMAGE_ALLOWED_HOSTS=cdn.shopify.com,images.asos-media.com
# PROXY_IMAGE_MAX_BYTES=8388608
# PROXY_IMAGE_TIMEOUT_MS=8000
# RATE_LIMIT_CREATE_BETA_PER_MIN=3
# RATE_LIMIT_CLAIM_BETA_PER_MIN=8
# RATE_LIMIT_LIST_BETA_PER_MIN=20
# RATE_LIMIT_PROXY_IMAGE_PER_MIN=10
# BETA_INVITE_ADMIN_EMAILS=admin1@yourapp.com,admin2@yourapp.com

# Helper: run `./scripts/fix-backend-secrets.sh` (or `supabase secrets set ...`) to copy these values
